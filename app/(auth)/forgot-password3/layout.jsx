export const metadata = {
  title: "Dastiyor - Service Marketplace",
  description: "Dastiyor - Online Services Marketplace Admin.",
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
